fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic-settings>=2.5,<3.0
httpx>=0.27,<1.0
supabase>=2.8,<3.0
pytest>=8.3,<9.0
# pyarrow intentionally NOT listed here: as of this writing it has no
# pre-built wheel for Python 3.14 (Render's current runtime), and building
# it from source fails (requires the system Arrow C++ library, which isn't
# present in Render's standard build image - see the CMake/Arrow_DIR error
# in the failed deploy log). app/services/export.py already handles pyarrow
# being absent gracefully: the app starts normally, every other export
# format works, and only /api/v1/export/parquet returns a clean 503
# ("temporarily unavailable, try CSV or JSON instead") instead of crashing
# the whole deploy. Re-add a version constraint here once pyarrow publishes
# cp314 wheels (or if Render's Python version is pinned back to <=3.12/3.13).
